
import SwiftUI

struct ListenNow: View {
    var body: some View {
        Text("Listen Now")
    }
}

struct ListenNow_Previews: PreviewProvider {
    static var previews: some View {
        ListenNow()
    }
}
